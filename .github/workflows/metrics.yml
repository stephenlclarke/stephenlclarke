---
name: Profile metrics

"on":
  workflow_dispatch:
  schedule:
    - cron: "17 4 * * *"
  push:
    branches:
      - main
    paths:
      - README.md
      - .github/workflows/metrics.yml

permissions:
  contents: write

jobs:
  metrics:
    runs-on: ubuntu-latest
    env:
      METRICS_TOKEN: ${{ secrets.METRICS_TOKEN }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Skip when METRICS_TOKEN is missing
        if: ${{ env.METRICS_TOKEN == '' }}
        run: echo "METRICS_TOKEN is not set; skipping Metrics generation."

      - name: Overview
        if: ${{ env.METRICS_TOKEN != '' }}
        uses: lowlighter/metrics@latest
        with:
          token: ${{ env.METRICS_TOKEN }}
          user: stephenlclarke
          filename: metrics.classic.svg
          output_action: none
          config_timezone: Europe/London
          config_display: large
          base: header, activity, community, repositories, metadata

      - name: Full-year activity
        if: ${{ env.METRICS_TOKEN != '' }}
        uses: lowlighter/metrics@latest
        with:
          token: ${{ env.METRICS_TOKEN }}
          user: stephenlclarke
          filename: metrics.isocalendar.svg
          output_action: none
          config_timezone: Europe/London
          base: ""
          plugin_isocalendar: "yes"
          plugin_isocalendar_duration: full-year

      - name: Languages
        if: ${{ env.METRICS_TOKEN != '' }}
        uses: lowlighter/metrics@latest
        with:
          token: ${{ env.METRICS_TOKEN }}
          user: stephenlclarke
          filename: metrics.languages.svg
          output_action: none
          config_timezone: Europe/London
          base: ""
          plugin_languages: "yes"
          plugin_languages_limit: 8
          plugin_languages_sections: most-used
          plugin_languages_details: percentage

      - name: Habits
        if: ${{ env.METRICS_TOKEN != '' }}
        uses: lowlighter/metrics@latest
        with:
          token: ${{ env.METRICS_TOKEN }}
          user: stephenlclarke
          filename: metrics.habits.svg
          output_action: none
          config_timezone: Europe/London
          base: ""
          plugin_habits: "yes"
          plugin_habits_facts: "yes"
          plugin_habits_charts: "yes"

      - name: Achievements
        if: ${{ env.METRICS_TOKEN != '' }}
        uses: lowlighter/metrics@latest
        with:
          token: ${{ env.METRICS_TOKEN }}
          user: stephenlclarke
          filename: metrics.achievements.svg
          output_action: none
          config_timezone: Europe/London
          base: ""
          plugin_achievements: "yes"
          plugin_achievements_display: compact
          plugin_achievements_threshold: X

      - name: Commit generated metrics
        if: ${{ env.METRICS_TOKEN != '' }}
        run: |
          set -euo pipefail
          sudo cp /metrics_renders/*.svg .
          bot_email="41898282+github-actions[bot]@users.noreply.github.com"
          git config user.name "github-actions[bot]"
          git config user.email "$bot_email"
          git add metrics.*.svg
          if git diff --cached --quiet; then
            echo "No metrics changes to commit."
            exit 0
          fi
          git commit \
            -m "chore: update profile metrics [skip ci]"
          git push
